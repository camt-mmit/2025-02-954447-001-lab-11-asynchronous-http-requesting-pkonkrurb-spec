import { ExtractNestedDatePipe } from './extract-nested-date-pipe';

describe('ExtractNestedDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractNestedDatePipe();
    expect(pipe).toBeTruthy();
  });
});
